# SHPEKBot

# SHPEKBot 🤖

Telegram Bot built with Python 3.11, Aiogram 3.x, and Docker.

## 📋 Prerequisites

Before you start, ensure you have **macOS** with [Homebrew](https://brew.sh/) installed.

## 🚀 Quick Start (Local Development)

We use a `Makefile` to automate the setup process.

### 1. Installation
Run one command to set up Python 3.11, Poetry, and all dependencies:
```bash
make install
```

### 2. Create .env file in root
```bash
BOT_TOKEN=...